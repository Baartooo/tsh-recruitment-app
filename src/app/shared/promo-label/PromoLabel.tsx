import React from 'react';

import s from './PromoLabel.module.scss';

export const PromoLabel = () => (
  <div className={s.promoLabel}>
    <span className={s.promoLabel__content}>Promo</span>
  </div>
);
